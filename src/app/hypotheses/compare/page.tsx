"use client";
import { useEffect,useState } from "react";
export default function CompareHypothesesPage(){const [all,setAll]=useState<any[]>([]);const [selected,setSelected]=useState<string[]>([]);const [comparison,setComparison]=useState<any|null>(null);useEffect(()=>{fetch('/api/hypotheses').then(r=>r.json()).then(d=>{setAll(d.hypotheses??[]);setSelected((d.hypotheses??[]).slice(0,2).map((h:any)=>h.id));});},[]);async function compare(){if(selected.length<2)return;const response=await fetch(`/api/hypotheses/compare?ids=${selected.join(',')}`);setComparison(await response.json());}function toggle(id:string){setSelected(current=>current.includes(id)?current.filter(value=>value!==id):current.length<6?[...current,id]:current);}return <main className="standard-page"><header className="page-header"><div><p className="eyebrow">Shared observations</p><h1>Compare hypotheses</h1></div><p>Place alternatives against the same predictions and evidence cards. Coverage is descriptive; it is never a truth score.</p></header><section className="teacher-picker">{all.map(h=><button aria-pressed={selected.includes(h.id)} onClick={()=>toggle(h.id)} key={h.id}>{h.title}<small>{h.state} · {h.evidence_count} cards</small></button>)}<button className="solid-button" disabled={selected.length<2} onClick={compare}>Compare selected</button></section>{all.length<2&&<p className="empty-state">Create at least two private hypotheses to compare them against shared observations.</p>}{comparison?.error&&<p className="error-box">{comparison.error.message}</p>}{comparison?.hypotheses&&<div className="comparison-table"><table><thead><tr><th>Dimension</th>{comparison.hypotheses.map((h:any)=><th key={h.id}>{h.title}</th>)}</tr></thead><tbody>{comparison.dimensions.map((dimension:string)=><tr key={dimension}><th>{dimension.replaceAll('_',' ')}</th>{comparison.hypotheses.map((h:any)=><td key={h.id}>{['supports','challenges','contextualizes','ambiguous','cannot_test'].includes(dimension)?h.evidence.filter((e:any)=>e.stance===dimension).map((e:any)=><span className="teacher-citation" key={e.id}>{e.statement}<small>{e.domain} · {e.source} · {e.locator}</small></span>):(h[dimension]??[]).map((value:string)=><p key={value}>{value}</p>)}</td>)}</tr>)}</tbody></table></div>}</main>}
