"use client";
import { useState,type FormEvent } from "react";
export function RightsReviewForm({sourceId,onSaved}:{sourceId:string;onSaved:()=>void}){const [message,setMessage]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/sources/${sourceId}/rights`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({lane:form.get('lane'),publication_allowed:form.get('publication_allowed')==='on',full_text_publication_allowed:form.get('full_text_publication_allowed')==='on',note:form.get('note'),license_name:form.get('license_name')||undefined,license_url:form.get('license_url')||undefined,attribution_text:form.get('attribution_text')||undefined})});const data=await response.json();setMessage(response.ok?'Rights decision saved.':data.error?.message??'Could not save');if(response.ok)onSaved();}return <form className="rights-form" onSubmit={submit}><h2>Rights review</h2><div className="form-grid"><div className="field"><label>Lane</label><select name="lane" defaultValue="yellow"><option value="green">Green</option><option value="yellow">Yellow</option><option value="red">Red</option></select></div><div className="field"><label>License name</label><input name="license_name"/></div><div className="field full"><label>License URL</label><input type="url" name="license_url"/></div><div className="field full"><label>Decision note</label><textarea required name="note"/></div><div className="field"><label><input type="checkbox" name="publication_allowed"/> Metadata / summary publication allowed</label></div><div className="field"><label><input type="checkbox" name="full_text_publication_allowed"/> Full text publication allowed</label></div><div className="field full"><label>Required attribution</label><textarea name="attribution_text"/></div></div><button className="solid-button">Save rights decision</button>{message&&<p>{message}</p>}</form>}

